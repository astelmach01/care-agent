import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="chat-header">
            <h1>Kouper Health Assistant</h1>
        </header>
    );
};

export default Header;
